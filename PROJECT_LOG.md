# ESP8266 项目交接日志

## 2026-09-06：TF 模式与运行时间分离、AI 动态识别

### 功能与结构

- `src/tf_mode_store.h/.cpp` 负责在 TF 卡 `/MODES.TXT` 中读取、新增和删除灯光模式。卡内模式只保存名称以及主机、A/B/C 从机的四个 LED 状态，不再保存固定时间。
- 空卡首次启动时自动写入“模式 1”和“模式 2”，基础模式不可从网页删除。TF 卡最多保存 12 个模式，名称最长 24 个字符。
- 文件仍采用七字段格式。第三个字段保留为 `0`，用于兼容已经部署的固件和现有 TF 文件，Flask 不再把它解释为持续时间。
- 文件更新先完整写入 `/MODES.NEW`，再用 `/MODES.BAK` 保留上一版后替换正式文件；若替换期间断电，下次启动会优先恢复备份。
- COM10 主机随状态上报 TF 卡可用状态和真实模式列表，Flask 通过带编号的幂等命令让主机执行新增或删除，收到主机回执后网页才显示写卡完成。
- 用户每次选中模式后，再填写本次运行时间，可在 1 至 3600 秒之间选择。同一个组合下次可换成另一个时间；倒计时结束后自动恢复阈值检测。
- 原来平铺在阈值区域的两个模式按钮已经移除。顶部新增“模式”入口，模式查看、新建、执行和删除全部位于独立窗口；访问 `/#modes` 可直接进入该窗口。
- 网页顶部显示 TF 卡当前是可用、写入中还是不可用。模式窗口同时显示物理卡内的模式数量和最近一次写卡错误。
- DeepSeek 的项目上下文现在包含四块 ESP8266 的分工、低电平点灯规则、A0 阈值、TFT、TF 卡状态、动态模式列表和当前倒计时。普通聊天、项目解释、排障和一般知识问答不受硬件动作白名单限制；只有真正执行设备动作时才校验允许的动作。
- AI 不再只认识 `mode_1` 和 `mode_2`。用户说出新建模式的名称并给出本次时间后，模型会从实时 `tf_card.modes` 中匹配编号；卡内不存在的编号仍会被 Flask 拒绝。

### 本次真实验证

- `master`、`slave_a`、`slave_b`、`slave_c` 四个 PlatformIO 环境全部编译成功。主机 RAM 使用 37.5%，Flash 使用 32.6%。
- 修复后的主机固件已用 921600 baud 下载至 COM10，写入哈希校验通过。
- UART0 实测输出 `[TF] SELF-TEST PASS` 和 `[TF] Mode file ready`。TF 卡容量约 14906 MiB，临时文件读写与清理均为 `PASS`；Wi-Fi 地址为 `192.168.124.12`。
- 首次下载后发现主机虽然开机读卡成功，后续 HTTP 报文却发送 `"tf_card_ready":false` 和空模式列表。抓取原始报文确认问题在固件缓存后，给 TF 模式访问函数加入了从 `/MODES.TXT` 自动恢复缓存的处理。再次编译下载后，Flask 持续收到 `tf_card_ready=true` 和两个真实模式。
- 同一个“模式 1”实测先运行 2 秒、再运行 5 秒，两次倒计时均采用本次提交的时间，结束后都恢复自动阈值检测。
- 实际写入临时模式“最终 AI 测试”后，DeepSeek 把名称识别为 `mode_custom_1`，执行 2 秒并自动恢复。删除命令获得主机回执，最终卡内只保留“模式 1、模式 2”。
- 12 项 Python 测试通过，覆盖动态模式白名单、项目状态上下文、可变运行时间、模式不保存时间、天气和时间问答。
- 7 项 Playwright 测试通过，覆盖 320、375、414、768、1280 px 页面宽度、聊天输入和模式执行请求。手机与桌面整页截图、375 px 模式窗口截图已人工检查，没有横向溢出、文字重叠或按钮裁切；图片保存在 `buffer/render_checks/`。
- Flask 已从项目虚拟环境重启并监听 `0.0.0.0:5000`。四台设备在线，TF 卡可用，自动阈值检测已恢复，没有测试模式或倒计时残留。

### 使用方法

- 在项目根目录执行 `server\.venv\Scripts\python.exe server\app.py`。`python-dotenv` 会自动读取被 Git 忽略的 `server/.env`，不需要在命令行粘贴 DeepSeek 密钥。
- 打开 `http://192.168.124.3:5000/`，在网页顶部点击“模式”。先选择卡内组合，再在“本次执行”区域填写秒数并点击“执行”；倒计时结束后自动恢复阈值检测。
- 在“新建模式”中填写名称并勾选四块设备中需要点亮的 LED，再点击“保存到 TF 卡”。新建时不填时间。窗口顶部从“正在写入 TF 卡”变为“TF 卡已连接”且列表出现新模式后，才表示物理写卡完成。
- 自定义模式右侧会显示删除图标；“模式 1”和“模式 2”是基础模式，不显示删除按钮。

## 2026-09-05：主机 TFT 卡槽读写自检

### 接线与自检实现

- 厂家 `1.8inch_SPI_MAR1801_Schematic.pdf` 显示，TF 卡槽通过独立四针接口引出 `SD_SS`、`SD_DI`、`SD_SCK`、`SD_DO`，不会随 TFT 的 11 针屏幕接口自动接入 ESP8266。
- 主机建议接线为 `SD_SS -> D2/GPIO4`、`SD_DI -> D7/GPIO13`、`SD_SCK -> D5/GPIO14`、`SD_DO -> D6/GPIO12`；D5/D7 与 TFT 共用 SPI，D2 作为 TF 卡独立片选。
- 新增 `src/tf_card_test.h/.cpp` 独立模块，只在主机固件中挂载 TF 卡、列出根目录、写入并读回 `/TF_TEST.TXT`，最后立即删除测试文件。
- 三个从机编译环境明确排除 `tf_card_test.cpp`，不会引入 SD 库或改变原有采样与灯光固件。
- TFT 开机时短暂显示挂载、读写、容量、根目录条目数和最终结果，3.5 秒后恢复原有主机监测画面；UART0 同时输出完整诊断。
- TFT_eSPI 已开启 `SUPPORT_TRANSACTIONS`，避免屏幕和 TF 卡共享 SPI 时同时占用总线。

### 本次实测结果

- 最终版四个环境均编译成功：`master`、`slave_a`、`slave_b`、`slave_c` 全部通过；主机 RAM 使用 37.0%，Flash 使用 32.3%。
- 最终版固件已通过 921600 baud 再次下载到 COM10，写入完成且固件哈希校验通过。
- 第二次复位实测时，串口仍输出 `[TF] FAIL: mount failed; check card format and four signal wires`，本次未能挂载 TF 卡，因此也没有进入文件写入步骤。
- 主机随后正常启动并连接 Wi-Fi，地址为 `192.168.124.12`，说明自检失败没有阻断原有主机功能。
- 当前最可能原因是只插入了 TF 卡但未连接独立四针 SD 接口；接好上述四根信号线后，按主机复位键即可再次运行相同自检。

## 2026-09-05：新增两种四灯组合模式

### 模式定义与执行

- 新增模式 1：A、B、C 三个从机 LED 全亮，主机 LED 灭。
- 新增模式 2：A、B、C 三个从机 LED 全灭，主机 LED 亮，与模式 1 完全相反。
- 两种组合模式都会自动关闭阈值检测并进入手动控制，服务端一次性写入四块设备的目标状态，避免逐灯操作产生短暂的中间状态。
- 现有 ESP8266 轮询协议已经能向每块板返回各自的 `manual_led`，因此本轮不需要修改、编译或下载固件。

### 网页与对话控制

- 阈值检测区新增“模式 1”和“模式 2”分段按钮；服务端会根据完整的四灯目标状态返回当前选中模式。
- 模式按钮带有灯光关系说明和 `aria-pressed` 选中状态；手机页面会自动换行，不挤压自动检测开关。
- DeepSeek 新增白名单动作 `set_preset`，用户输入“切换到模式 1”或“切换到模式 2”时可以直接应用对应组合。
- 任意单灯手动控制仍然可用；当四灯状态不再完整匹配组合预设时，两个模式按钮都不会显示为选中。

### 验证结果

- 八项 Python 自动化测试通过，覆盖两种组合状态的原子写入、自动转手动及 DeepSeek 动作白名单。
- 七项 Playwright 测试通过，覆盖 320、375、414、768、1280 px 布局，以及模式 1、模式 2 的真实点击和选中态切换。
- 实际接口验证模式 1 返回主机灭、A 亮，各设备配置读取一致；模式 2 返回主机亮、A 灭且阈值检测关闭。
- 桌面和 320 px 手机全页截图已人工检查，新增控制组无横向溢出、文字重叠或按钮换行。
- 测试完成后已恢复自动阈值检测；Flask 继续监听 `0.0.0.0:5000`。

## 2026-09-05：精简对话输入与统一能力提示

### 对话区调整

- 保持现有 `Feature Stack / Cobalt` 页面结构、配色、字体和设备信息布局不变。
- 删除对话区启动时自动显示的“我在。”，网页刚打开时聊天记录为空，只有用户真正发送消息后才出现对话气泡。
- 删除麦克风按钮及全部浏览器 `SpeechRecognition` 代码，避免局域网 HTTP、浏览器兼容性和麦克风权限导致入口可见但不可用。
- “朗读”开关和浏览器 `speechSynthesis` 回复朗读功能继续保留，不受语音输入删除影响。
- 发送按钮改为独立右对齐的 44 px 图标按钮，桌面与手机布局均保持稳定。

### 提示文案与安全边界

- 新增统一提示常量 `UNSUPPORTED_FUNCTION_MESSAGE`，内容为“不支持当前功能。”。
- 用户要求修改阈值、网络、ADC、采样率或执行白名单以外的动作时，提示“不支持当前功能。”，不会再显示“DeepSeek 返回无效”或类似内部校验描述。
- DeepSeek 返回不可执行的意图、动作或结构时仍由 Flask 拒绝，修改只影响面向用户的提示，不放宽设备控制白名单。
- 天气源数据异常改为“实时天气数据暂不可用。”，与真正不支持的功能区分。

### 验证结果

- 六项 Python 自动化测试通过，其中越界聊天动作会精确返回“不支持当前功能。”。
- Python 编译检查、JavaScript 语法检查和 `git diff --check` 均通过。
- 六项 Playwright 测试通过，覆盖 320、375、414、768、1280 px 无横向溢出，以及初始聊天记录为空、页面不存在麦克风按钮。
- 桌面和 320 px 手机全页截图已人工检查，未发现布局溢出、重叠或视觉风格变化。
- Flask 已使用项目 `buffer/qa_venv` 重新启动并监听 `0.0.0.0:5000`；本轮未修改、编译或下载 ESP8266 固件。

## 2026-09-05：通用对话、实时天气与 Apple 风格界面

### 对话与意图判断

- DeepSeek 助手现在会自行区分 `chat`、`control`、`time`、`weather` 四类意图，不再把所有输入都当作灯光指令。
- 普通聊天可以直接自然回答；网页会携带最近八条对话消息，因此可以理解简短的连续追问。
- 时间由 Flask 注入带 UTC+8 时区的当前服务器时间，模型只能依据该时间回答，不再依赖模型猜测。
- 设备控制仍只允许切换自动/手动模式，以及控制主机、A、B、C LED；聊天、时间和天气意图一律不能携带设备动作。
- 控制计划继续由 Flask 做角色、模式、动作类型和布尔值白名单校验，聊天能力没有放宽硬件执行权限。

### 实时天气

- 新增 `server/weather_service.py`，先用 Open-Meteo 地理编码解析用户所说的城市，再按坐标获取实时天气和当天预报，不需要额外天气 API Key。
- 天气数据包括观测时间、天气现象、当前与体感温度、湿度、风速、当前降水、今日高低温和最高降水概率。
- DeepSeek 只负责把可信天气 JSON 整理成自然中文，不允许凭模型知识编造实时数据。
- 天气网络请求遇到瞬时 TLS 或连接中断时最多尝试三次；无法获取时返回明确错误，不伪造结果。
- 实测“南京现在天气如何？”正确识别为天气意图，从 Open-Meteo 获得南京实时观测，并且没有执行任何 LED 动作。

### 朗读与界面

- 浏览器朗读会优先选择系统中的云扬、云希、康康等自然中文男声，语速设为 0.9、音调设为 0.78，使声音更低沉平稳。
- 项目没有复制电影角色或演员的专属声纹；最终自然度取决于访问设备实际安装的系统语音，浏览器没有高质量中文语音时仍会回退。
- 页面按 Hallmark 重新设计为 `Feature Stack / Cobalt`：桌面左侧固定对话区，右侧集中显示模式、四机状态和 ADC 波形；手机按相同任务顺序单列显示。
- 使用浅色中性背景和少量蓝色状态信号，不使用渐变、装饰图或营销式大标题；Lucide 图标已保存为本地静态资源，断开 CDN 也不会阻塞控制脚本。
- 页面控件保留键盘焦点、按压、禁用、加载、错误和成功状态，并支持减少动态效果设置。

### 验证结果

- Python 编译检查、JavaScript 语法检查和 `git diff --check` 均通过。
- 六项 Python 自动化测试通过，覆盖聊天禁止控制、天气禁止控制、合法动作白名单、八条历史限制、实时天气路由和 UTC+8 时间上下文。
- 六项 Playwright 浏览器测试通过，覆盖 320、375、414、768、1280 px 布局、无横向滚动、本地 Lucide 渲染和对话输入交互。
- 桌面与 320 px 手机全页截图已人工检查，未发现文字、控件或设备卡片重叠。
- Flask 已重新启动并监听 `0.0.0.0:5000`；本机访问 `http://127.0.0.1:5000/`，同一路由器设备访问 `http://192.168.124.3:5000/`。
- 本轮未修改 ESP8266 固件，因此没有重新编译或下载四块开发板。

## 2026-09-05：时间与天气问答诊断

- 当前 DeepSeek 被定义为 ESP8266 灯光控制助手，只允许切换自动/手动模式，以及控制主机、A、B、C 的 LED。
- Flask 只向模型传入检测模式、A 从机阈值结果、四块设备的在线状态和 LED 状态，没有传入服务器当前时间。
- 项目没有接入天气 API，也没有提供城市位置或联网搜索工具；DeepSeek 对话接口本身不会自动获得实时天气。
- 因此询问时间或天气时，模型会按现有安全边界将其视为不支持的其他请求，而不是通用问答。
- 若后续扩展，时间可由 Flask 获取本地时间后注入对话上下文；天气需增加城市来源和可靠的天气数据接口，同时继续保留 LED 动作白名单校验。

## 2026-09-05：DeepSeek 对话与语音控制

### 接口与安全边界

- Flask 新增 `server/deepseek_assistant.py`，使用 DeepSeek 的 OpenAI 兼容对话接口和 `deepseek-v4-flash` 模型。
- API Key 只保存在本机 `server/.env`；该文件已加入 `.gitignore`。仓库仅保留不含密钥的 `server/.env.example`。
- DeepSeek 只能提出两类动作：切换自动/手动模式，或设置主机、A、B、C 的 LED 状态。
- Flask 会再次校验动作类型、角色、模式和布尔值。未知动作、无效角色、超长回复和超过 5 项的计划全部拒绝。
- 一组动作会先完整检查再执行，避免前几项已生效、后续动作才报错。
- 单个访问端每 60 秒最多调用 10 次 AI，减少误触造成的 API 消耗。

### 网页对话

- 网页增加 DeepSeek 对话区，可输入“切换到手动模式，打开 B 从机，关闭 C 从机”等自然语言指令。
- DeepSeek 回复和 Flask 的实际执行结果会同时显示；设备状态仍由原有 500 ms 轮询刷新。
- 支持浏览器 `SpeechRecognition` 时显示语音按钮，识别到中文后自动提交。
- “朗读回复”开启时使用浏览器 `speechSynthesis` 播放中文回复。
- 语音识别并非所有浏览器都支持，局域网 HTTP 页面也可能被浏览器限制麦克风权限；文字输入始终可用。

### 验证结果

- 提供的 API Key 已通过 DeepSeek `/models` 和 `/chat/completions` 实际请求验证，真实密钥未写入源码、日志或 Git。
- 状态询问返回空动作；非法角色和自动模式下单灯控制会被本地校验拒绝。
- 实测对话“切换到手动模式，打开 B 从机，关闭 C 从机”正确执行为手动模式、B 亮、C 灭。
- 随后通过对话“恢复自动阈值检测”恢复自动模式；四块 ESP8266 仍在线。
- HTML 已确认加载文字输入、语音按钮、回复朗读和聊天记录区域，JavaScript 与 Python 语法检查通过。
- 当前 Windows 会话无法生成 Edge 无头截图，Computer Use 也没有可用浏览器实例，因此没有完成截图级视觉检查。
- 密钥曾在聊天中明文发送。完成当前测试后应在 DeepSeek 控制台轮换，并更新本机 `server/.env`。

## 2026-09-05：扩展为一主三从

### 设备与固件

- COM10 为主机，编译环境是 `master`。
- COM11 为从机 A，编译环境是 `slave_a`；A 保持 1000 Hz 目标采样率读取 A0。
- COM12 为从机 B，编译环境是 `slave_b`；B 暂不读取或上传 ADC。
- COM13 为从机 C，编译环境是 `slave_c`；C 暂不读取或上传 ADC。
- 四块设备的 LED 均为低电平点亮，程序内部仍以 `true` 表示灯亮。

### 控制规则

- 自动检测模式只使用 A 从机的 A0 数据判断是否超过 0.600 V。
- 主机、A、B、C 在自动模式下均跟随 A 的阈值结果亮灭，B/C 不参与阈值计算。
- 切换为手动模式后，网页可分别控制主机、A、B、C 的 LED，四块设备互不影响。
- Flask 设备角色扩展为 `master`、`slave_a`、`slave_b`、`slave_c`；服务端拒绝 B/C 上传 ADC 数据。

### 主机 TFT 与网页

- 主机 TFT 显示 Wi-Fi 状态、A 从机电压、检测模式，以及 A/B/C 三块从机各自的 LED 实际状态。
- TFT 六行内容均限制在 128×160 屏幕范围内，每 500 ms 更新一次。
- 网页已改为四机阈值控制台，提供四张独立设备卡；A 显示 ADC 数据，B/C 标明 ADC 暂不启用。
- 自动模式下网页禁用手动按钮；进入手动模式后，四块设备的开关按钮分别生效。

### 编译、下载与实测

- `master`、`slave_a`、`slave_b`、`slave_c` 四个环境全部编译通过。
- 固件已分别下载至 COM10、COM11、COM12、COM13，四次写入均通过 Flash 哈希校验。
- 下载后四块设备全部在线：主机 `192.168.124.12`、A `192.168.124.11`、B `192.168.124.13`、C `192.168.124.14`。
- 实测时 A 已超过阈值，自动模式下主机和 A/B/C 四灯均回报为亮。
- 手动模式实测组合为主机灭、A 亮、B 灭、C 亮，四块设备均正确回报；随后已恢复自动检测。
- A 从机近 5 秒实收采样率约为 937–938 Hz。
- Flask 已启动并监听 `0.0.0.0:5000`；本机地址为 `http://127.0.0.1:5000/`，局域网地址为 `http://192.168.124.3:5000/`。
- 页面接口、DOM 标识和 JavaScript 语法检查通过；本次环境没有可用浏览器实例，因此未完成截图级视觉检查。

## 2026-09-04：统一 LED 为低电平触发

- 主机 D0/GPIO16 和从机 GPIO4 现在都采用低电平点亮、高电平熄灭。
- 网页及程序内部仍统一使用 `true` 表示灯亮；`led_control` 模块负责将开灯状态转换为实际的 `LOW` 输出。
- 主机和从机固件均编译通过，并分别下载到 COM10、COM11；两次写入均通过 Flash 哈希校验。
- 手动模式实测两块设备都能执行开灯和关灯指令，测试结束后已恢复自动阈值检测。

### 主机 1.8 寸 TFT

- TFT 安装在 COM10 主机，使用厂家资料指定的 ST7735、128×160 配置。
- 接线为 SCK=D5、MOSI/SDA=D7、DC/RS=D3、RST=D4、CS=D8；主机 LED 仍使用 D0，两者没有引脚冲突。
- 新增 `src/tft_display.h/.cpp` 独立模块，每 500 ms 显示主机 Wi-Fi、从机 A0 电压、阈值开关、0.600 V 阈值、是否超限和主机 LED 状态。
- Flask 的设备配置响应新增 `slave_adc_raw`，主机利用原有 500 ms 状态请求取得从机最新 A0，不额外发送 HTTP 请求。
- 厂家 `ESP82661.8TFT.ino` 保留作接线和 API 参考，但关闭独立的 `setup()/loop()`，避免与正式项目入口重复。
- `TFT_eSPI 2.5.43` 只加入 `master` 编译环境，不进入从机固件，因此不影响从机高速采样任务。

### 本次实测

- 主机 IP 为 `192.168.124.12`，从机 IP 为 `192.168.124.11`，两块设备下载后均恢复在线。
- TFT 所需接口成功返回从机最新 ADC 数据；检查时原始值为 5，对应 0.005 V。
- 从机目标采样率仍为 1000 Hz，下载后近 5 秒实收采样率约为 942–958 Hz。
- Flask 已重启并继续监听 `0.0.0.0:5000`。

## 2026-09-04：高速采样与双机下载

### 采样优化

- 从机 A0 采样周期由 4 ms 改为 1000 us，目标采样率从 250 Hz 提高到 1000 Hz。
- `adc_sampler` 改用 `micros()` 计时，采样缓冲区从 128 点扩大到 512 点。
- 从机不再额外执行 `delay(1)`。ESP8266 Arduino 核心在每轮 `loop()` 返回后按需运行 Wi-Fi 协议栈。
- 状态 POST 的响应现在直接带回阈值和手动控制配置，设备不再额外发送配置 GET 请求。
- Flask 保存最多 20000 个 ADC 点，向网页返回时抽取约 2000 点绘制，避免浏览器处理 15 秒的全部高速数据。
- 网页采样率改为显示近 5 秒实收值，同时标出 1000 Hz 目标值。

### 下载和实测结果

- 主机固件已下载到 COM10，写入校验通过，设备 IP 为 `192.168.124.12`。
- 从机固件已下载到 COM11，写入校验通过，设备 IP 为 `192.168.124.11`。
- 两台设备均能持续向 Flask 上报，自动阈值模式已恢复为开启状态。
- 稳定运行时，Flask 近 5 秒实收采样率约为 935–960 Hz，达到 1000 Hz 目标的约 94%–96%。
- 15 秒波形窗口实测跨度为 14999 ms，窗口内曾收到 12734 个原始采样点。
- 手动控制闭环已验证：关闭阈值检测后，网页指令可让主机灯和从机灯分别亮、灭；测试结束后已恢复自动检测。
- 页面已再次检查 320、375、414、768、1280 px 宽度，均无横向滚动，实时波形正常显示。

### 当前服务

Flask 已使用项目虚拟环境在后台运行，监听 `0.0.0.0:5000`。本机访问 `http://127.0.0.1:5000/`，同一路由器设备访问 `http://192.168.124.3:5000/`。

## 2026-09-04：双机阈值监测版本

### 设备分工

- 主机编译环境为 `master`，预留下载端口 `COM10`。主机通过 Flask 读取从机阈值状态，控制 D0/GPIO16 LED，低电平点亮。
- 从机编译环境为 `slave`，预留下载端口 `COM11`。从机读取 A0，控制 GPIO4 LED，低电平点亮。
- 两块设备不直接用串口通信。它们连接同一路由器，通过电脑上的 Flask 服务交换状态和配置。

### 控制规则

- 阈值检测开启时，从机将 A0 与 0.600 V 阈值比较。超过阈值后，从机 GPIO4 灯点亮；主机从 Flask 取得结果后点亮 D0，并上报“电压大于阈值”。
- 阈值检测关闭时，网页可以分别打开或关闭主机、从机的 LED。
- 自动模式下服务端拒绝手动 LED 指令，避免两套控制逻辑互相覆盖。
- 当前按 ESP8266 A0 满量程 1.0 V、10 位 ADC 换算，0.600 V 对应原始值 614。若开发板 A0 前端自带分压，必须按实际板卡量程修改换算关系。

### 新的源码模块

- `src/device_config.h`：主从角色、引脚、电平极性、采样和通信周期。
- `src/wifi_manager.h/.cpp`：路由器连接和断线重连。
- `src/adc_sampler.h/.cpp`：从机 A0 定时采样和环形缓冲。
- `src/led_control.h/.cpp`：统一处理高电平或低电平点亮的 LED。
- `src/server_client.h/.cpp`：读取 Flask 配置并上报主机或从机状态。
- `src/main.cpp`：根据 `DEVICE_ROLE_MASTER` 编译主机或从机任务调度代码。

### 编译与验证

- `platformio run -e master -e slave` 编译通过。
- 主机固件：RAM 35.0%，Flash 27.6%。
- 从机固件：RAM 35.3%，Flash 27.6%。
- 本次按要求没有向 COM10 或 COM11 下载固件，因此真实双机联动尚未验证。
- Flask 接口模拟通过：A0 原始值 500 未超阈值，700 超过阈值；自动模式拒绝手动指令，关闭阈值后允许分别控制设备。
- 网页已在 320、375、414、768、1280 px 宽度检查，无横向滚动。

### 编译命令

```powershell
platformio run -e master
platformio run -e slave
```

下载时必须明确指定环境，避免把角色烧错：主机使用 `platformio run -e master --target upload`，从机使用 `platformio run -e slave --target upload`。

### 如何切换主机和从机

项目只有一个 `src/main.cpp`，不需要手动替换文件。`platformio.ini` 中的两个编译环境会传入不同的预处理标志：

- `[env:master]` 传入 `DEVICE_ROLE_MASTER=1`，编译 `main.cpp` 中 `#if DEVICE_ROLE_MASTER` 下的主机代码。
- `[env:slave]` 传入 `DEVICE_ROLE_MASTER=0`，编译 `#else` 下的从机代码。

在 VS Code 左侧打开 PlatformIO，进入 `PROJECT TASKS`，选择 `master` 或 `slave`，再点对应环境下的 `Build`。以后需要下载时，也必须在对应环境下点 `Upload`。

不带 `-e` 执行 `platformio run` 时，因为 `default_envs = master, slave`，PlatformIO 会把两个固件都编译一遍。

两个板同时连接时，执行 `platformio run --target upload` 会按 `default_envs` 的顺序处理两个环境：先用 `DEVICE_ROLE_MASTER=1` 生成主机固件并写入 COM10，再用 `DEVICE_ROLE_MASTER=0` 生成从机固件并写入 COM11。PlatformIO 不会自动判断板子的角色，角色完全由环境标志和端口绑定决定。

第一次下载建议分开执行：

```powershell
platformio run -e master --target upload
platformio run -e slave --target upload
```

下载前应在 Windows 设备管理器或 PlatformIO Devices 中确认主机仍是 COM10、从机仍是 COM11。更换 USB 插口或驱动后，COM 编号可能变化；端口写反会把两块板的角色固件烧反。

## 2026-09-04

### 当前功能

- ESP8266 连接路由器，并向 Flask 服务上报 GPIO4、设备 IP、RSSI、运行时间和 A0 原始采样值。
- GPIO4 默认每秒翻转一次；收到网页远程指令后进入远程控制模式。
- A0 配置为 4 ms 一次采样，服务端按 250 Hz 显示最近 15 秒波形。
- Flask 页面显示设备在线状态、GPIO4 状态、A0 当前值、最小值、最大值和波形。
- Flask 页面提供 GPIO4“打开、关闭、翻转”按钮。

### 远程控制链路

网页向 `POST /api/command` 发送 `{"action":"on"}`、`{"action":"off"}` 或 `{"action":"toggle"}`。

ESP8266 每 250 ms 请求 `GET /api/command`。服务端返回待执行 LED 状态后，设备设置 GPIO4，并通过下一次 `POST /api/status` 回报结果。服务端收到目标状态后清除待执行指令。

### 源码结构

- `src/main.cpp`：系统初始化、任务调度、Wi-Fi、ADC 采样和状态上报。
- `src/led_control.h/.cpp`：GPIO4 初始化、自动闪烁、远程控制状态。
- `src/command_client.h/.cpp`：从 Flask 轮询并解析 LED 指令。
- `server/app.py`：Flask 页面、状态接口、波形缓存和指令接口。
- `server/templates/index.html`：页面结构和 GPIO4 控制按钮。
- `server/static/app.js`：状态轮询、波形绘制和指令发送。
- `server/static/styles.css`：页面布局和控件样式。

### 已验证事项

- PlatformIO 编译成功，目标板为 ESP8266 ESP-12E。
- 固件已通过 `COM9` 下载，上传速率为 921600。
- ESP8266 实测地址为 `192.168.124.11`，电脑 Flask 地址为 `192.168.124.3:5000`。
- 服务端接口测试通过：`on`、`off` 指令返回 200，无效 action 返回 400。
- 真实硬件闭环测试通过：服务端下发 `off` 后设备回报低电平，下发 `on` 后设备回报高电平。

### 启动方法

在项目根目录执行：

```powershell
cd server
.\.venv\Scripts\python.exe app.py
```

保持该 PowerShell 窗口打开，然后访问 `http://192.168.124.3:5000/`。电脑和 ESP8266 必须连接同一路由器。

### 交接注意事项

- `include/wifi_secrets.h` 含 Wi-Fi 配置，已由 `.gitignore` 排除，不要提交到公开仓库。
- 修改 Flask 监听端口或电脑 IP 后，要同步修改 `src/main.cpp` 的状态地址和 `src/command_client.cpp` 的指令地址。
- 修改固件后，在项目根目录执行 `platformio run` 编译；确认无误后执行 `platformio run --target upload` 下载。
- 项目临时文件、编译缓存和测试日志放在 `buffer/`，不要把这些文件移到 C 盘临时目录。
